<br/>
<br/>
		<div class="box box-50 altbox">
			<div class="boxin">
				<div class="header">
					<h3>You are already Connected to <%=params[:id].humanize%></h3>
				</div>

<% form_tag oauth_consumer_path(params[:id]),:method=>:delete do %>
  <%= submit_tag "Disconnect" , :class => 'button altbutton' %>
  or
  <%= submit_tag "Reconnect" , :class => 'button altbutton' %>
  if you experienced a problem.
<% end %>

			</div>
		</div>
